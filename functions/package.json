{
  "name": "tile-blast-functions",
  "private": true,
  "description": "Cloud Functions — autoridade econômica (ads + IAP Play)",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "lint": "node -e \"require('./iap-logic.js'); require('./confirm-iap.js'); require('./score-logic.js'); require('./challenge-logic.js'); require('./delete-social.js'); console.log('functions modules ok')\"",
    "serve": "echo 'Deploy: firebase deploy --only functions (requer firebase-tools + projeto)'"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.1.0",
    "googleapis": "^144.0.0"
  }
}
